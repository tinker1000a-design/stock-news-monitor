# 股票新闻监控系统 · Agent 自举手册（BOOTSTRAP）

> **本文件面向任何接手本系统的 AI（无论前后是谁）。按章节顺序执行，不跳步。**
> 配套：《股票新闻监控系统-设计文档.md》（设计原理）｜ `config.yaml`（唯一配置源）｜ `templates/`（报告模板）

---

## 0. 系统一句话

追踪股票新闻 → 按 短期/中期/长期（可配置）维度筛选 → 生成「日期+核心命题」日报，周五收盘后出周报、月报季报递进 → 全部报告写入 Obsidian vault 形成五维双链知识图谱 → 命题登记表定期回看打分，防止过拟合与讨好。

## 0.5 获取本仓库（网络降级三级方案）

GitHub 在国内为"间歇性可直连"，部署前先判定连通性，按序降级：

**判定**：`curl -sI --connect-timeout 8 https://github.com` 返回 HTTP 2xx/3xx 即视为可达。

| 级别 | 条件 | 动作 |
|------|------|------|
| L1 直连 | 判定通过 | `git clone https://github.com/tinker1000a-design/stock-news-monitor.git` |
| L2 镜像 | 直连超时/失败 | 在原始 URL 前加镜像前缀 clone，按序尝试：`https://ghfast.top/`、`https://gh-proxy.com/`（镜像站可能失效，连不上换下一个） |
| L3 逐文件抓取 | clone 全部失败 | 用 curl/WebFetch 逐个抓 `https://raw.githubusercontent.com/tinker1000a-design/stock-news-monitor/main/<路径>`（也可加 L2 镜像前缀），按清单写盘：`BOOTSTRAP.md`、`config.template.yaml`、`设计文档.md`、`templates/日报模板.md`、`templates/周报模板.md`、`templates/个股中枢模板.md`、`templates/命题登记模板.md`。写盘后目录结构对齐本仓库，继续执行本手册 |

**注意**：部署完成后系统日常运行不依赖 GitHub（数据源是东财 MCP），本节仅在首次部署/更新时使用。抓取的文件内容若明显残缺（截断/乱码），换镜像重试，不得将残缺手册当作执行依据。

---

## 1. 环境检测与安装（首次运行必做）

| 依赖 | 检测方法 | 缺失时动作 |
|------|----------|------------|
| Obsidian | 查 `%LocalAppData%\Obsidian\Obsidian.exe` 或注册表 Uninstall 项 | `winget install Obsidian.Obsidian`；失败则告知用户手动装（系统不依赖 Obsidian 运行，只依赖 vault 目录，可先建目录后补装） |
| 东财数据源 | 调用东财 MCP（妙想/westock 等）拉一条测试行情 | **按序降级**：①其他已连接的金融 MCP；②免费直连源（akshare / mootdx / 新浪财经 HTTP 接口，python 标准库+pip 可装）；③全部不可用才出"数据源不可用"日报。**禁止凭模型记忆补数据** |
| 运行环境 | python 或 node 任一可用 | 报告生成本身是 AI 任务，无重依赖；仅 CSV/JSON 处理用标准库 |

**A股特化检查清单**（筛选每条新闻时逐项过，命中即标注维度）：
- **解禁/限售**：解禁日、解禁比例、股东减持公告（中期维度强信号）
- **政策**：行业政策、监管动向、交易所规则变化
- **游资/龙虎榜**：席位异动、资金性质（短期维度）
- **交易语境**：解读价格信号时必须考虑 T+1、涨跌停、ST 规则、最小手数（如"涨停无法买入"这类约束会改变信号含义）

### 1.1 数据源口径备忘（westock CLI · 2026-09-24 实测补充）

本机实际数据源为 **westock CLI**（腾讯自选股数据接口，含板块/资金/技术/公告/研报）。以下四点每次运行前必须过一遍，否则会重蹈已踩过的坑：

| # | 备忘 | 后果（若忽略） |
|---|------|----------------|
| 1 | **不在 PATH 里**：二进制位于 `C:/Users/admin/.local/bin/westock.exe`。调用前先 `export PATH="/c/Users/admin/.local/bin:$PATH"` | 报 `command not found`，误判为"数据源不可用" |
| 2 | **`MainNetFlow5D / 10D / 20D` 是"截至上一交易日（T-1）"的滚动窗口，不含请求当日** | 同一天不同时点读到不同数，被误判为"口径冲突"。**拐点日必须另算单日值**：`westock fund flow <code> --start <起> --end <止>` 拉逐日自行求和（2026-09-24 已用逐日复算精确验证此规则） |
| 3 | **同一次 `quote` 返回的字段顺序不固定**（接口按字段可用性重排表头），映射值必须按**表头逐列对齐**，禁止按固定位置猜 | 张冠李戴（如把 `chg_5d` 读成 `chg_ytd`） |
| 4 | **上市不足 120 个交易日的个股，`MA120/MA250` 会返回值若干填充值**（长鑫 2026-09-24 返回的 MA120/MA250 与 MA60 同为 55.41） | 把填充值当作"多均线共振"，虚构支撑强度 |

**双源交叉验证的可用路径**（本系统要求关键数据 ≥2 独立来源）：
- 第一源：westock CLI（行情/资金/板块/技术）
- 第二源（任选，优先前两项）：①`WebSearch` 检索当日收评（东方财富财富号/证券时报·数据宝/中国商报/湖南日报等会给出指数、成交额、涨跌家数、行业涨跌与个股资金排名）；②腾讯行情页（`gu.qq.com/<code>` 的打开页，可复核 OHLC/换手/量比/资金结构）；③证券时报·数据宝系媒体稿
- **成交额口径**：`westock changedist` 的"两市成交额"= 沪+深，**不含北交所**；媒体常报的"1.67 万亿"多为**沪深京三市**。两者差约 150-200 亿，**不是冲突**，引用时必须标注含不含北交所。

## 2. 用户画像问卷（自适应激活 · 首跑必做）

系统不预设用户是谁。**首次部署时必须先完成本问卷，再初始化骨架**——画像决定激活哪套预设，不同画像生成的是不同的报告重心和节奏。

### 2.0 问卷（逐题问，合并为一次对话更好）

| # | 问题 | 选项 |
|---|------|------|
| 1 | 主策略类型？ | A 长期价值投资 / B 中线波段 / C 短线投机博弈 / D 混合（按标的区分，需指定每只的风格） |
| 2 | 信息节奏？ | 盘中即时 / 每日收盘一次 / 每周一次 |
| 3 | 关注面权重排序？ | 基本面 / 资金面 / 情绪面 / 政策面（排前三） |
| 4 | 持仓集中度？ | 重仓少数（≤5 只，深挖型）/ 分散跟踪（广撒网型） |

答案写入 `config.yaml` 的 `profile:` 段。**问卷不得省略，用户不答就逐题追问；全部默认值只允许在用户明确说"你看着办"时使用，且报告中标注"当前画像为默认假设，可随时改"**。

### 2.1 画像 → 预设激活规则

| 画像 | 激活的行为差异 |
|------|----------------|
| **A 长期价值** | 维度重心=长期+中期；日报侧重基本面/竞争格局/护城河变化，新增"估值锚点"栏（PE/PB 分位+口径标注）；命题验证窗口=季报/年报节点；龙虎榜/游资类信号默认折叠；日报节奏可放宽至每周汇总 |
| **B 中线波段** | 短期+中期均衡；侧重资金流向+事件催化+回调买点识别；命题窗口=1-4 周；节奏=每日收盘；涨跌停/T+1 语境必标 |
| **C 短线投机** | 短期主导；需盘中快讯能力（无盘中数据源时明示降级，不得用收盘数据冒充盘中）；侧重情绪周期/连板梯队/龙虎榜；命题窗口=1-5 日；**强制附加噪声警告**：该模式下未核实信息占比天然偏高，警告线降至 0.3 |
| **D 混合** | 每只标的在 `stocks[].style_override` 指定 A/B/C，各标的独立激活 |

### 2.2 画像的边界（冷通道不变）

画像改变的是**分析视角、排序、节奏、栏目构成**——即"看什么、怎么看、多快看"。它**不改变**：证据分级标准、反方论点必填、措辞黑名单、命题登记打分。短线画像也不会让"未核实传闻"变成买入依据。预设细节见 `config.template.yaml` 的 `presets:` 段，用户画像与预设冲突时以用户显式要求为准并回写 config。

## 3. Vault 发现与初始化

### 3.1 定位 vault（按序尝试，取第一个成功）
1. **读 config**：`vault.path` 非空且目录存在 → 直接用。
2. **读 Obsidian 注册表文件**：`%APPDATA%\obsidian\obsidian.json` 内含用户所有已注册 vault 路径 → 取列表；仅一个则用之，多个则向用户展示列表让其选。
3. **扫描常见位置**：在各盘根（C/D/E）及 `%USERPROFILE%\Documents` 下查找含 `.obsidian` 子目录的文件夹（深度 ≤ 2，避免全盘扫）。
4. **兜底自建**：以上全失败 → 在 `%USERPROFILE%\Documents\股票监控图谱` 创建目录并写入空的 `.obsidian/` 文件夹（Obsidian 打开该目录即识别为 vault），告知用户路径。
5. 无论走哪条路，把最终路径写回 `config.yaml` 的 `vault.path`。

### 3.2 初始化骨架

1. 读 `config.yaml` 的 `vault.path`。为空 → 询问用户 vault 路径，写入 config 后继续。
2. 按以下骨架创建目录（已存在则跳过，不覆盖）：

```
<vault>/
├── 个股/          # 每只注册标的一个中枢笔记
├── 事件/          # 一事一记
├── 行业/          # 产业链锚点
├── 主题/          # 跨行业题材
├── 日报/  周报/  月报/
├── 命题登记/      # 可证伪命题，一题一文件
└── .monitor/      # AI 运行状态（用户无需关心）
    └── state.json
```

3. 写入 `state.json` 初始状态（schema 见第 6 节）。
4. 每只 `config.stocks` 里的标的：用 `templates/个股中枢模板.md` 建中枢笔记；其所属行业/主题笔记若不存在则一并创建。
5. 把 `templates/命题看板.base` 复制到 vault 根目录（Obsidian 1.9+ 的 Bases 原生看板：打开即见"全部命题/待观察"两张视图，无需任何插件）。

## 4. 标的注册（扩容的唯一入口）

**新增任意一只股票 = 只改 config.yaml + 跑一次注册流程**，不写任何硬编码逻辑：

1. 在 `config.yaml` 的 `stocks:` 下追加条目（code/name/alias/priority）。
2. 检查其产业链、主题笔记是否存在，不存在则创建并互链。
3. 用中枢模板生成 `个股/<名称>.md`，回链到行业与主题。
4. 若用户指定了偏好（priority/风格），仅写入 config——记住：偏好只影响排序与篇幅，**永不进入结论**（见第 7 节）。

## 5. 自动化注册（弹窗确认，零对话轮）

**触发时机：用户给出任意一只追踪标的、注册流程（第 4 节）完成的同一回合内。**

**禁止行为**：不得用文字问"要现在挂上定时任务吗？"——这会多一轮对话。必须用交互弹窗（WorkBuddy `AskUserQuestion`，其他环境用等价的选择 UI）让用户点选：

> 问题：`为 <股票名> 开启定时追踪任务？`
> 选项：
> 1. **开启双任务（推荐）**——每日收盘后新闻日报 + 每周五复盘周报
> 2. **仅每日日报**——先跑新闻采集，周报以后再说
> 3. **暂不开启**——仅手动触发

用户点选后**立即执行**，不再追问。全默认场景（用户说"你看着办"）= 直接选 1 并在完成汇报中注明。

### 任务定义（两个 recurring，已存在同名任务则更新不新建）

**任务 1 · 新闻追踪日报** `FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=16;BYMINUTE=5`
prompt 必须自包含：先读本仓库 BOOTSTRAP.md 第 6-7 节流程；标的与别名列表；vault 路径 `<config.vault.path>`；拉取当日新闻/公告/资金流→三维筛选→按日报模板写 `<vault>/日报/YYYY-MM-DD-<标的>-<核心命题>.md`→登记可证伪命题→更新个股中枢时间线→回写 state.json。数据源按第 1 节降级链，不可用时写"数据源不可用"日报，禁止凭记忆补数据。报告 status: final 后按 **PUSH.md** 规则推送摘要卡到微信端（失败重试→记录 state.json.push_log→下次补推，不得阻塞）。

**任务 2 · 周五复盘周报** `FREQ=WEEKLY;BYDAY=FR;BYHOUR=15;BYMINUTE=30`
prompt 必须自包含：扫描 `<vault>/命题登记/` 下 `结果: 待观察` 且 `验证截止 <= 今天` 的命题**逐条打分**（成立/证伪/待观察，一个不许跳）；汇总本周日报；盲区引擎三项（覆盖矩阵/反向信息注入/证据漏斗）；按周报模板写 `<vault>/周报/YYYY-WW-周报.md`；证伪信息回写图谱（连续证伪 3 次的信源降权）。报告 status: final 后按 **PUSH.md** 推送周报摘要卡（必含命题打分统计：成立 X / 证伪 Y / 待观察 Z）。

**可选任务 3 · 早报**（`config.schedules.morning.enabled=true` 时注册）`FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=8;BYMINUTE=30`
prompt 必须自包含：拉取**隔夜**国际动态（美股三大指数及中概、美联储/央行动态、大宗商品与美元指数、地缘政治与贸易政策、中概股ADR表现）→ 映射到追踪标的（按 source_trust 过滤与分级）→ 输出"对今日 A 股开盘的预期影响"（判定+区间，可证伪表述）→ 写 `<vault>/早报/YYYY-MM-DD-早报.md` → 推送摘要卡。
**周一特例（强制）**：周一早报的采集窗口为**周六 00:00 至周一早间**整个双休区间，必须先读上一周末的两篇周末简报（任务 5 产物）作为输入，把双休期间的公告/政策/产业新闻一并纳入影响分析——双休信息断档会导致周一开盘判断失真，禁止只看"隔夜一晚"。

**可选任务 4 · 晚报**（`config.schedules.evening.enabled=true` 时注册）`FREQ=WEEKLY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=18;BYMINUTE=0`
prompt 必须自包含：复盘**白天**盘面（A 股收盘全景、板块资金流、追踪标的当日表现与公告、盘后重要信息）→ 与早报的预期影响做**对照打分**（预期 vs 实际，喂给命题回路）→ 写 `<vault>/晚报/YYYY-MM-DD-晚报.md` → 推送摘要卡。若早报未生成（节假日顺延等），晚报单独生成并在文首注明。

**可选任务 5 · 周末新闻简报**（`config.schedules.weekend.enabled=true` 时注册）`FREQ=WEEKLY;BYDAY=SA,SU;BYHOUR=10;BYMINUTE=0`
prompt 必须自包含：周末**无盘面但新闻不断**——上市公司公告（业绩预告/重大合同/增减持多在周五盘后至周日发布）、监管与政策发布、海外市场（美股周五收盘/周日晚开盘动态）、行业与产业新闻。拉取自上周五收盘以来的全部信息 → 按 source_trust 过滤分级 → 按"信息面（发生了什么）/ 影响面（对标的与板块的可能传导）/ 待验证命题（周一开盘前的观察点）"三段写 `<vault>/周末简报/YYYY-MM-DD-周末简报.md` → 登记新命题 → 推送摘要卡。**只采集分析，不做盘面复盘**（无交易数据），判定一律标注"非交易日，等待周一开盘验证"。

**非交易日通用规则**：法定节假日（工作日但 A 股休市）当天早报/晚报降级为"节假日新闻简报"（同任务 5 三段式，写 `<vault>/周末简报/`，文件名含"节假日"），不做盘面复盘、不推送"预期 vs 实际"对照；节后首个交易日的早报同样适用**周一特例**——采集窗口回溯覆盖整个假期。是否为交易日以实际数据源返回的行情为准判断，不硬编码节假日表。

**任务 6 · 手动综合汇总**（非定时，用户对话触发，无弹窗直接执行）

触发词（用户说任意一个即触发，可带参数）：「汇总本周」「综合汇总」「专题汇总 XX」「把 XX 一起汇总」。

- **范围解析**：默认 = 本自然周的全部报告（早报/晚报/周末简报/日报，按 `config.vault.path` 对应目录扫描）+ 本周线索箱条目；用户可指定时间段（"最近两周"）、标的（"只看长鑫"）、主题（"存储板块"）覆盖默认范围。
- **输入源**：①指定范围的所有系统报告；②`<vault>/线索箱/` 下范围内的全部条目；③**用户在触发对话中当场补充的信息**（"我今天看到…"）——先按线索箱格式落盘为条目，再进汇总，不许只留在对话里。
- **汇总规则**：用户线索与系统采集过同一套 source_trust 分级，**不因是用户自己提供的而免检**——用户口述无出处的信息默认证据等级"未核实"，只可做参考背景，不得进核心结论；汇总报告必须有一节"用户线索 vs 系统报告的交叉印证"，指出哪些相互印证、哪些冲突、哪些是系统盲区外的新信息。
- **输出**：写 `<vault>/汇总/YYYY-MM-DD-综合汇总-<主题>.md`，结构：核心结论（判定四选一）→ 各信息源时间线合并 → 交叉印证表 → 修正后的命题（登记/更新命题登记）→ 盲区提示。status: final 后按 **PUSH.md** 推送。
- **频次不设限**：这是用户手动开关，一天多次也执行；与周五自动周报互不替代（周报含命题打分与盲区引擎，手动汇总含用户线索，可互相引用）。

**线索箱（用户信息注入的唯一入口）**

- 目录：`<vault>/线索箱/`。用户随时可以：对话里说"我看到 XX"（AI 代写条目）或自己往目录丢笔记（运行时钩子第 6 节会自动识别新文件）。
- 条目格式：`YYYY-MM-DD-<线索主题>.md`，frontmatter 必填：`来源:`（用户口述/用户转述某处/用户实测）、`verified: 未核实|有来源但争议|已交叉验证`、`关联标的/行业/主题:`（AI 按五维链接规则自动挂 `[[双链]]`）。
- 纪律：**存用户的观察，不存用户的结论**——用户说"我觉得要涨"时记录为"用户倾向：看多"，同时把支撑其判断的事实单独提取；倾向可进参考，不可进结论。这是"不讨好"原则在线索箱的延伸。

（启用早报/晚报后，原 16:05 日报是否保留由用户选择：并存或替代。）

（可选第三任务 · 月报 `FREQ=MONTHLY;BYMONTHDAY=-1;BYHOUR=16;BYMINUTE=30`：命中率统计，用户在弹窗"其他"里要求时才创建。）

## 6. 运行时钩子：增量识别更新的笔记（核心）

`.monitor/state.json` schema：

```json
{
  "version": 1,
  "last_run": "2026-09-23T16:05:00",
  "processed": { "个股/长鑫科技.md": { "mtime": 0, "hash": "..." } },
  "pending_analysis": [],
  "config_hash": "..."
}
```

**每次运行的钩子流程：**

0. **首次运行判定**：`state.json` 中存在 `"seed-"` 前缀的 hash/mtime → 视为种子状态，先对全部种子笔记计算真实 mtime+hash 重建 `processed`，并计算 config 真实哈希回写 `config_hash`，然后继续第 1 步。**state.json 不存在 → 按 2.2 重建骨架后再运行。**
1. 读 `state.json` + 计算 `config.yaml` 哈希 → 不一致说明配置被改过，先走第 4 节校验（新标的注册/维度变更）。
2. 扫描 `watch_dirs` 内所有 `.md` 的 mtime，与 `processed` 对比 → 新增/变更笔记进入 `pending_analysis`。
3. frontmatter 钩子字段（新 AI 写笔记时必须遵守）：
   - `updated: <ISO时间>` —— 手动/外部修改笔记时应更新它；
   - `status: draft | final` —— 仅 `final` 笔记参与综合报告，`draft` 提示"待完善"；
   - `verified: 已交叉验证 | 单一来源 | 未核实` —— 证据等级。
4. 分析完成后回写 `state.json`（更新 processed、清空 pending、写 last_run）。
5. **禁止事项**：不得绕过 state.json 做全量重扫后不重建状态文件；不得删除 `.monitor/`；写 state.json 用"临时文件 + 原子替换"（先写 `.tmp` 再改名），避免半写损坏。

**到期命题扫描规则（周报/月报打分入口）**：扫描 `命题登记/` 下所有笔记的 frontmatter，筛出 `结果: 待观察` 且 `验证截止 <= 本次报告日期` 的文件 → 全部列入打分表，一个都不能跳过。扫不到任何到期命题也要在周报明确写"本周无到期命题"。

**重复观点判定算法**：对本期与上期核心命题句做关键词抽取（实体+动词，去停用词），计算 Jaccard 重合度 ≥ `thresholds.similarity_repeat_warn` 即判"重复观点"。判定结果连同两组关键词一并写入报告，供复核。

这套钩子保证：后续任何 AI（或用户手动改笔记后）都能被系统识别，增量进入下一轮分析，而不是重复处理或漏处理。

## 7. 报告生成流程

1. **先搜后写**：grep vault 已有笔记做实体匹配，链接到已存在节点，禁止造重复节点。
2. 按 `templates/日报模板.md`（周报/月报同理）填空，`{{dimensions}}` 从 config 渲染。
3. 核心命题写入 `命题登记/命题-<日期>-<序号>.md`，必须是**可证伪表述**（含验证方式+预期）。
4. 证据黑名单措辞自动检查：`config.anti_sycophancy.wording_blacklist` 命中即改写。
5. 写入报告 → 更新个股中枢"事件时间线" → 回写 state.json。
6. **微信推送**：报告 `status: final` 后按 `PUSH.md` 推送摘要卡；`push.channel=none` 或 webhook 为空则跳过并写一行日志，不报错。

## 8. 客观性红线（任何 AI 不可豁免）

### 8.1 信源黑名单与可信度（用户配置驱动，见 config.source_trust）

- **黑名单**：`blacklist` 命中的信源**完全丢弃**——不进"未核实"层级、不进图谱、不进推送；仅在日报"已过滤信源"栏留痕（只记条数，不转述内容）。用户说"不采信 XX / 讨厌 XX 的信息"→ 当轮写入 config 黑名单（这是用户明确授权的修改，直接执行）。
- **可信度覆盖**：`credibility` 按信源覆盖默认分级，分数影响该信源信息的呈现权重与证据等级判定，**不改变**"未核实不进结论"红线。
- **自动降级**：信源信息在命题打分中被证伪累计达 `auto_demote_falsified` 次 → 可信度自动降一级并在 MEMORY 记录，与既有"连续证伪降权"机制合并。

### 8.2 其余红线

- **冷热分离**：偏好字段（priority、用户风格）用于排序与篇幅，结论生成时视为不可见。
- **反方论点必填**：模板该栏为空必须写明"检索了什么关键词、未找到"。
- **分级证据**：自媒体/转载 = 未核实，不进结论；未核实占比 > `thresholds.evidence_unverified_ratio_warn` 时在报告顶部警告。
- **无新增信息要明说**：与上期结论相似度 > `similarity_repeat_warn` → 标"重复观点"，不重写。
- **连续证伪降权**：同一信源/主题连续 `consecutive_falsified_demote` 次命题证伪 → 后续报告中自动降权标注。
- **数据源断供**：降级为"数据源不可用"，禁止用训练记忆补当日数据。

## 9. 修改容差

- **维度增删/改窗口**：只改 `config.yaml` 的 `dimensions`，模板用占位符渲染，报告自动带上新维度栏。
- **报告频率调整**：只改 `schedules` 并更新对应 automation。
- **配置校验失败**（schema 不合、字段缺失）：保守运行 + 日报顶部标记"配置异常：<原因>"，不得静默忽略。

## 10. 用户意图对齐记忆（对话循环钩子，设计见 INTENT.md）

**这是唯一由对话本身驱动的记忆层**：绝大多数轮次零动作，只在用户表达"不满意 / 想修改"时激活。

运行时每个用户轮次的固定检查（开销极低）：

1. 读 `<vault>/.monitor/intent/MEMORY.md`（启动时一次，之后内存持有）。
2. 对每条用户消息跑触发判据（INTENT.md 第 1 节：显式修正 / 隐式不满 / 修改请求；拿不准 → 不激活）。
3. 触发 → 四步闭环：捕获对象 → 3 句摘要存当日 `.monitor/intent/YYYY-MM-DD.md` → 合并去重更新 MEMORY.md（条目格式与三条纪律见 INTENT.md 第 2 节）→ **当轮立即修正输出**并注明"已按你的反馈调整"。
4. 边界（不可越权）：意图记忆只改**形式**（格式/深度/排序/篇幅/推送样式），永不改客观性红线；改 config.profile 需用户明确同意。

初始化：第 3.2 节建目录时一并创建 `.monitor/intent/` 与种子 MEMORY.md（三条默认假设：结论前置 / 中文摘要 ≤60 字 / 未核实信息须标注）。主档 ≤3000 字，超限先蒸馏失效条目。

## 11. 新用户指南（部署完成时必须展示）

很多用户第一次接触这类系统，拿到后不知道方向。**每次完成部署（或用户说"不知道怎么用"时），必须做两件事：**

### 11.1 展示指南图
向用户展示本仓库 `docs/用户指南.png`（混元绘制的一图流指南：系统四步流程 + 六条可执行命令方向）。

### 11.2 粘贴命令卡（纯文本兜底，任何环境可用）

```
【股票新闻监控系统 · 快速上手】
它做的事：新闻采集 → 智能筛选 → 日报周报 → Obsidian 知识图谱

你可以直接对 AI 说：
· 部署系统            ← 首次使用，AI 会先问你 4 个画像问题
· 追加追踪股票 XX     ← 任意标的，自动建笔记+图谱链接
· 生成今日日报        ← 手动触发一次完整分析
· 本周命题打分        ← 让 AI 回看之前预言对了还是错了
· 查看我的盲区        ← 看看哪些角落从没被新闻覆盖
· 修改我的画像        ← 换策略风格（价值/波段/短线）

报告位置：Obsidian vault 的 日报/ 周报/ 目录 + 命题看板.base
规矩：未核实的信息不会进结论；AI 找不到反方证据也要交代查了什么。
```

部署收尾时先展示图，再贴命令卡原文，最后问一句"要现在生成第一篇日报吗？"
