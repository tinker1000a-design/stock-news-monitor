# INTENT.md · 用户意图对齐记忆层（反馈触发式）

> 核心目的只有一个：**让后续输出对齐用户真实意图**。它不是聊天记录归档，不是画像收集器——绝大多数对话轮次它完全沉默。

## 1. 激活条件（唯一钩子：负反馈）

**只在用户对当前系统产出表达"不满意 / 想修改"时激活**，其余任何轮次（提问、闲聊、确认、夸奖）一律不激活、不记录。

触发判据（满足其一即激活）：
- **显式修正**："不对""错了""不要这样""改成 X""别再 Y""重写""太啰嗦/太长/太浅"
- **隐式不满**：连续追问同一问题（说明上一轮没对齐）；用户用自己的话复述一遍再问"是这个意思吗"（说明表述偏离其意图）
- **修改请求**：对格式、深度、维度、判定口径、推送内容等提出的任何变更要求

**不激活的情况（防过拟合）**：正面评价、中性提问、新话题、用户明确说"ok/行/可以"。夸奖不写入记忆——系统只从错误中学习，不从恭维中过拟合。

保守原则：**拿不准是否算不满时，宁可当作普通轮次**（最多在心里记一句"疑似待观察"），不落盘。

## 2. 存储（vault 内、图谱外）

位置：`<vault>/.monitor/intent/`（点目录 → Obsidian 图谱不渲染，不污染股票知识图谱；文件仍在笔记库里可随时翻阅）

```
.monitor/intent/
  MEMORY.md          # 意图对齐主档（≤3000字，滚动维护，改写不追加）
  2026-09-23.md      # 当日触发轮的对话摘要（追加式，30 天前蒸馏进主档后删除）
```

### MEMORY.md 条目格式（每条必须可执行、可验证）

```markdown
- [2026-09-23] 偏好：日报判定栏放最前，不要最后 | 来源：用户对首篇日报说"结论埋太深" | 状态：生效中
- [2026-09-23] 偏好：推送摘要不要带"机会提示"字样 | 来源：修改请求 | 状态：生效中
- [日期] 偏好：<一条规则> | 来源：<触发轮摘要链接> | 状态：生效中 | 已失效→<日期>（被某条取代/用户撤回）
```

三条纪律：
1. **存规则不存情绪**：写"日报结论前置"，不写"用户嫌报告写得烂"。
2. **单条单一职责**：一条只记一个可执行偏好，方便单独失效。
3. **有寿命**：规则被新规则覆盖或用户撤回时标"已失效"不删除——保留演变史，防止 AI 摇摆。

## 3. 对话循环嵌入点（运行时流程）

每个用户轮次的固定动作（开销极低，通常零操作）：

```
用户消息 → 触发判据检查（第 1 节）
  ├─ 未触发 → 正常处理，记忆层零动作
  └─ 触发 → 四步闭环：
      1. 捕获：定位本轮对话中"用户不满意的具体对象"（哪份报告/哪个字段/哪种口径）
      2. 摘要：3 句以内写清"用户原话要点 + 推导出的偏好规则"，存入当日 .md
      3. 提炼：与 MEMORY.md 已有规则合并去重 → 更新主档（改动处标注日期）
      4. 应用：**当轮立即修正输出**（不等下一次报告），并在修正后的产出末尾注明
         "已按你的反馈调整：<规则摘要>"
```

## 4. 与其他层的边界（防越权）

| 边界 | 规则 |
|------|------|
| vs 画像（config.profile） | 意图记忆可**提议**修改画像（如"用户反复要求更短窗口 → 建议 profile 改 spec"），但改 config 需用户明确同意；记忆本身不直接改配置 |
| vs 客观性红线 | 意图记忆**只能**改形式（格式/深度/排序/篇幅/推送样式），**永不**改证据分级、反方论点必填、措辞黑名单——用户哪怕说"我不想看反方观点"，也回复"这条是系统红线，但可以把它折叠到报告末尾"并记下折中方案 |
| vs 知识图谱 | 意图记忆在 `.monitor/`，永不生成 `[[双链]]` 进图谱 |
| vs 每次对话 | 未触发轮次 AI 不得"顺手"写记忆；主档超 3000 字时先蒸馏失效条目再写入 |

## 5. 冷启动

新部署 vault 无 `.monitor/intent/` 时：BOOTSTRAP 第 3.2 节建目录 + 写入种子 MEMORY.md（三条通用默认：结论前置、中文摘要 ≤60 字、未核实信息须标注）——种子条目同样是"可被用户反馈覆盖的假设"，不是永久规则。
