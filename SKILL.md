---
name: stock-news-monitor
description: 自适应股票新闻监控系统。部署时先通过用户画像问卷（策略类型/信息节奏/关注面）激活对应预设——长期价值、中线波段、短线投机或混合，再按画像生成差异化的日报周报（日期+核心命题），写入 Obsidian vault 形成双链知识图谱。内置命题登记打分回路（防过拟合）、冷热分离与反方论点必填（防讨好）、覆盖矩阵盲区引擎。当用户要求部署股票新闻监控、生成股票日报周报、复盘股票新闻、或维护股票知识图谱时使用。
---

# Stock News Monitor · 股票新闻监控系统

本 Skill 是完整系统的入口，采用渐进披露结构：

## 部署（从未部署的环境开始）

读本仓库根目录的 **`BOOTSTRAP.md`**，严格按章节顺序执行：

1. **0.5 节** 获取源码（GitHub 直连 → 镜像 → 逐文件抓取三级降级）
2. **1 节** 环境检测安装（Obsidian、数据源按序降级、A股特化检查清单）
3. **2 节** 用户画像问卷（策略类型/节奏/关注面 → 激活 value/swing/spec 预设）
4. **3 节** vault 发现/创建与骨架初始化（config.yaml → vault.path）
5. **4-5 节** 标的注册 + 定时任务注册
6. **6-9 节** 增量钩子（`.monitor/state.json`）、报告生成、客观性红线、修改容差

## 配置

- **唯一配置源**：`config.template.yaml` → 复制为 `config.yaml`（后者含个人路径，不入库）
- 标的、维度、阈值、调度、反讨好黑名单全部在 config.yaml，无硬编码

## 模板

`templates/` 下四个模板 + `命题看板.base`（Obsidian Bases 原生看板）：
- 日报模板（含强制判定栏 + 反方一回合辩论）
- 周报模板（到期命题打分 + 盲区栏目）
- 个股中枢模板 / 命题登记模板（可证伪命题）

## 红线（任何环境不可豁免）

偏好只管排序不管结论；反方论点必填；未核实来源不进结论；数据源断供时明示降级，禁止用模型记忆补数据；连续证伪 3 次的信源自动降权。
